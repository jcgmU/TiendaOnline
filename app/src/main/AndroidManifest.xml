<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Permisos necesarios -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TiendaOnline">

        <!-- LoginActivity como Launcher -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.TiendaOnline">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Clave de API de Google Maps -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="KEY API GOOGLE MAPS" />

        <!-- Otras actividades -->
        <activity android:name=".CheckoutActivity" android:exported="true" />
        <activity android:name=".PaymentPageActivity" android:exported="false" />
        <activity android:name=".EditarPerfilActivity" android:exported="true" />
        <activity android:name=".UsuariosMapaActivity" android:exported="true" />
        <activity android:name=".UsuarioInicioActivity" android:exported="true" />
        <activity android:name=".MainActivity" android:exported="true" android:label="@string/app_name" android:theme="@style/Theme.TiendaOnline" />
        <activity android:name=".RegistroActivity" android:exported="true" />
        <activity android:name=".ListadoProductosActivity" android:exported="true" />
        <activity android:name=".CarritoActivity" android:exported="true" />
        <activity android:name=".AdminPanelActivity" android:exported="true" />
        <activity android:name=".GestionProductosActivity" android:exported="true" />
        <activity android:name=".AgregarProductoActivity" android:exported="true" />
        <activity android:name=".EditarProductoActivity" android:exported="true" />
        <activity android:name=".GestionUsuariosActivity" android:exported="true" />
        <activity android:name=".PerfilActivity" android:exported="true" android:label="Perfil" android:theme="@style/Theme.TiendaOnline" />
    </application>
</manifest>
